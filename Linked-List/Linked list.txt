Reversing LL

void revLL(node* &head){
    node*current=head,*next,*prev=NULL;
    while(current!=NULL){
        next=current->next;
        current->next=prev;
        prev=current;
        current=next;
    }
    head=prev;
    
}